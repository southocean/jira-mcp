{
  "name": "jira-mcp-dist",
  "version": "1.1.1",
  "private": true,
  "description": "Build & release tooling for the standalone Jira MCP server",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "release": "node release.mjs"
  }
}
